{
    "properties" :{{ properties | raw }}, 
    "errors":{{ errors|json_encode(constant('JSON_PRETTY_PRINT'))|raw }},
    "warnings":{{ warnings|json_encode(constant('JSON_PRETTY_PRINT'))|raw }}
}